# Plugin marketplace

Repo-scoped plugin marketplace files live here.

`marketplace.json` exposes the reviewed native `ars-codex` plugin and Research
Book Skills from local submodule paths. Keep the registry tracked;
external-skill setup refreshes repository-owned metadata when needed. Both
entries remain `AVAILABLE`; setup does not install either plugin into a user's
Codex profile.
