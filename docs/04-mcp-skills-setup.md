# MCP and skills setup

MCP connects agents to local tools through controlled permissions.

Safe categories:

- filesystem
- Zotero
- browser or search
- Git
- shell
- database

Use read-only access by default. Raise permissions only for a bounded task.

