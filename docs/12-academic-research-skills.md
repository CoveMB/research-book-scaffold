# Academic Research Skills

The Academic Research Skills suite is external and optional.

Planned location:

```text
vendor/academic-research-skills/
```

The installer will discover folders containing `SKILL.md` and can copy usable skills into `.agents/skills/` with an `ars-` prefix when needed.

Do not run vendored scripts automatically. Review external instructions before use.

