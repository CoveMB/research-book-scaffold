# Scripts

Local setup, checks, rendering, and helper scripts live here.

Use `bash scripts/doctor.sh` for a quick health check, `python3 -m unittest discover scripts/tests` for script tests, and `make audit` for repository checks. `python3 scripts/setup_environment.py` treats the project root as the Obsidian vault root by default.
