# Scripts

Local setup, checks, rendering, and helper scripts live here.

Use `bash scripts/doctor.sh` for a quick health check, `python3 -m unittest discover scripts/tests` for script tests, and `make audit` for repository checks.
